{
  "name": "mcp-agent",
  "version": "0.1.0",
  "description": "AI-powered autonomous scraper agent for ASP data collection",
  "private": true,
  "scripts": {
    "dev": "python3 main.py",
    "setup": "pip install -r requirements.txt && playwright install chromium",
    "test": "pytest tests/",
    "lint": "ruff check .",
    "format": "black .",
    "docker:build": "docker build -t mcp-agent .",
    "docker:run": "docker run --env-file .env mcp-agent"
  }
}
